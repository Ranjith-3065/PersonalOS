<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PersonalOS | Dashboard</title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Inter', sans-serif;
}

body {
  background: #020617;
  color: #e5e7eb;
  display: flex;
  height: 100vh;
  overflow: hidden;
}

/* Overlay */
.overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  opacity: 0;
  pointer-events: none;
  transition: 0.3s;
  z-index: 9;
}

.overlay.show {
  opacity: 1;
  pointer-events: all;
}

/* Sidebar */
.sidebar {
  width: 240px;
  background: #0f172a;
  padding: 24px;
  display: flex;
  flex-direction: column;
  z-index: 10;
}

.brand {
  font-size: 22px;
  font-weight: 600;
  color: #38bdf8;
  margin-bottom: 30px;
}

.nav a {
  display: block;
  padding: 12px 14px;
  border-radius: 10px;
  color: #9ca3af;
  text-decoration: none;
  margin-bottom: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.nav a:hover {
  background: #111827;
  color: #38bdf8;
  transform: translateX(4px);
}

/* Main */
.main {
  flex: 1;
  padding: 30px;
  overflow-y: auto;
  animation: fadeIn 0.6s ease;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Header */
.header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 30px;
}

.menu-btn {
  display: none;
  font-size: 22px;
  background: transparent;
  border: none;
  color: #e5e7eb;
  cursor: pointer;
}

.header h1 {
  font-size: 22px;
}

/* Grid */
.grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 22px;
}

/* Section */
.section {
  background: #111827;
  padding: 22px;
  border-radius: 16px;
  min-height: 220px;
  cursor: pointer;
  transition: all 0.35s ease;
  position: relative;
  overflow: hidden;
}

.section::after {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at top left, rgba(56,189,248,0.15), transparent 60%);
  opacity: 0;
  transition: 0.4s;
}

.section:hover::after {
  opacity: 1;
}

.section:hover {
  transform: translateY(-6px);
  box-shadow: 0 25px 60px rgba(0,0,0,0.5);
}

.section h2 {
  font-size: 16px;
  margin-bottom: 14px;
}

/* Empty State */
.empty {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  font-size: 14px;
  color: #9ca3af;
  line-height: 1.6;
}

/* AI pulse */
.ai-core {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 140px;
}

.ai-orb {
  width: 60px;
  height: 60px;
  background: #38bdf8;
  border-radius: 50%;
  animation: pulse 2s infinite;
  box-shadow: 0 0 25px rgba(56,189,248,0.6);
}

@keyframes pulse {
  0% { transform: scale(1); opacity: 0.8; }
  50% { transform: scale(1.15); opacity: 1; }
  100% { transform: scale(1); opacity: 0.8; }
}

/* Mobile */
@media (max-width: 768px) {
  body {
    overflow-x: hidden;
  }

  .menu-btn {
    display: block;
  }

  .sidebar {
    position: fixed;
    left: -260px;
    top: 0;
    height: 100vh;
    transition: 0.35s ease;
  }

  .sidebar.open {
    left: 0;
  }

  .main {
    padding: 20px;
  }

  .grid {
    grid-template-columns: 1fr;
  }
}
</style>
</head>

<body>

<div class="overlay" onclick="toggleSidebar()"></div>

<!-- Sidebar -->
<div class="sidebar">
  <div class="brand">PersonalOS</div>
  <div class="nav">
    <a>Dashboard</a>
    <a>AI Assistant</a>
    <a>Habits</a>
    <a>Vault</a>
    <a>Timeline</a>
    <a href="/personalOS/profile">Settings</a>
    <a href="/personalOS/login">Logout</a>
  </div>
</div>

<!-- Main -->
<div class="main">

  <div class="header">
    <button class="menu-btn" onclick="toggleSidebar()">☰</button>
    <h1>Your Personal Intelligence Hub</h1>
  </div>

  <div class="grid">

    <div class="section">
      <h2>🤖 AI Assistant</h2>
      <div class="ai-core">
        <div class="ai-orb"></div>
      </div>
      <div class="empty">
        Ask anything, plan your day,<br>
        or control your PersonalOS
      </div>
    </div>

    <div class="section">
      <h2>🧠 Habits</h2>
      <div class="empty">
        No habits yet.<br>
        Build consistency over time ✨
      </div>
    </div>

    <div class="section">
      <h2>🔐 Vault</h2>
      <div class="empty">
        Vault is empty.<br>
        Secure your private data 🔒
      </div>
    </div>

    <div class="section">
      <h2>🕒 Timeline</h2>
      <div class="empty">
        No activity yet.<br>
        Your actions will appear here ⏳
      </div>
    </div>

  </div>
</div>

<script>
function toggleSidebar() {
  document.querySelector('.sidebar').classList.toggle('open');
  document.querySelector('.overlay').classList.toggle('show');
}
</script>

</body>
</html>
